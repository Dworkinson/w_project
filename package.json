{
  "name": "bsg",
  "version": "0.0.1",
  "description": "api_automation",
  "main": "index.js",
  "scripts": {
    "generateEmptyToken": "ts-node src/helpers/generateEmptyTokens.ts",
    "stg:comm": "mocha --ENV staging --MODE common --require ts-node/register --timeout 60000",
    "stg:test": "mocha --ENV staging --MODE test --require ts-node/register --timeout 60000",
    "stg:demo": "mocha --ENV staging --MODE demo --require ts-node/register --timeout 60000",
    "dev:comm": "mocha --ENV dev --MODE common --require ts-node/register --timeout 60000",
    "dev:test": "mocha --ENV dev --MODE test --require ts-node/register --timeout 60000",
    "dev:demo": "mocha --ENV dev --MODE demo --require ts-node/register --timeout 60000",
    "getToken:stage": "npm run stg:comm src/tests/token.test.ts",
    "accounts:stage": "npm run stg:comm src/tests/accounts.test.ts",
    "campaigns:stage": "npm run stg:comm src/tests/campaigns.test.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/minimist": "^1.2.2",
    "@types/mocha": "^9.1.1",
    "@types/node": "^18.0.0",
    "axios": "^0.27.2",
    "chai": "^4.3.6",
    "minimist": "^1.2.7",
    "mocha": "^10.0.0",
    "ts-node": "^10.8.1",
    "typescript": "^4.7.4"
  }
}
